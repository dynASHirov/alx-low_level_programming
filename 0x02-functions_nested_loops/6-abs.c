#include "main.h"
#include <stdio.h>
/**
 *_abs - absolute value
 @c: int for argument of the function
 *Return:0
 */
int _abs(int z)
{
	if (z > 0 || z ==0)
	{
	return (z);
	}
	else
	return (z * -1);
}
